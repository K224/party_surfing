script src="/nicedit/nicEdit.js"

javascript:
  $(document).on('page:load', function(){window['rangy'].initialized = false;})
  $(document).ready(function() {  new nicEditor().panelInstance('profile_contacts'); });

.container
  h1.textCenter Редактирование профиля
  .row
    .col-md-4
    .col-md-8.textBlack
      = semantic_form_for @profile do |f|
        =f.inputs :name, :surname, :birthday
        =f.inputs do
          =f.input :contacts, :label => false
        =f.actions
