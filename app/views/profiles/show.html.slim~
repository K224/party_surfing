.container
  .row
    .col-md-4
      div.ava.margin10 
      -if can? :update, @profile
        div.btn.btn-primary#editProfile.margin10bot =link_to "Редактировать", edit_profile_path(current_user)
        div.btn.btn-primary#creatParty.margin10bot =link_to "Создать мероприятие", new_party_path
      div.BlackBox.textCenter.margin10bot
        |Принимал участие
        -cnt = 0
        -@profile.user.participations.each do |par|
          -if par.accepted
            -cnt += 1
            div.WhiteBox =link_to par.party.title, par.party
        -if cnt == 0
          div.WhiteBox Не участвовал :(

      div.BlackBox.textCenter.margin10bot
        |Мои мероприятия
        -cnt = 0
        -@profile.user.parties.each do |party|
          div.WhiteBox =link_to party.title, party
          -cnt += 1
        -if cnt == 0
          div.WhiteBox Не создавал :(


    .col-md-7.col-md-offset-1
      div.margin10 
        h1 #{@profile.name} #{@profile.surname}
      div.margin10 #{@profile.age}
      div.margin10 
        | Контактная информация:
        /div=raw @profile.contacts
      div.margin10
        -if user_signed_in?
          div
            |Оставить комментарий
            =semantic_form_for @comment, url: comment_profile_path do |f|
              =f.inputs :title, :comment
              =f.actions
          hr/
        |Комментарии:
        -@profile.comments.each do |comment|
          div
            div =comment.title
            div =comment.comment
            div =comment.user.profile.full_name
          hr/
